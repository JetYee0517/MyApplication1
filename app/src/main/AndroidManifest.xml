<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/CustomTheme">
        <activity android:name=".PayPage"></activity>
        <activity
            android:name=".ActivityTreeMain"
            android:label="Planting"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".MyTrees"
            android:label="My Trees"
            android:parentActivityName=".MyTrees" />
        <activity
            android:name=".Tree"
            android:label="Trees"
            android:parentActivityName=".Tree" />
        <activity
            android:name=".FBContent"
            android:label="F&amp;B"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>